#ifndef SORTING_ARRAY_H
#define SORTING_ARRAY_H
#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int make_sorting_array(int* sorting_array, int array_size);
int bubblesort_pass(int* sorting_array, int endpoint);

#endif
